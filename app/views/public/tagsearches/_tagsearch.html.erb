<% Topic.order(created_at: 'desc').limit(20).each do |topic| %>
   <% if topic.category.present? %>
      <div class="col-1 m-1 btn btn-info btn-sm">
        #<%= link_to topic.category,tagsearches_search_path(content: topic.category), class: "text-white" %>
      </div>
   <% end %>
<% end %>

